# Passed to the extension build (also accepts VITE_API_URL).
API_URL=http://localhost:3001
# Web dashboard opened from the popup (Vite default).
VITE_DASHBOARD_URL=http://localhost:5173
# Optional: auto-fill extension storage with a dev bearer token when empty (never commit real tokens).
# VITE_API_BEARER_TOKEN=
